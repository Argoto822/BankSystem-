<Window x:Class="BankSystem.Views.ClientSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Выбор клиента" Height="400" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ВЫБОР КЛИЕНТА" FontSize="16" FontWeight="Bold" 
                   Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <TextBox x:Name="txtSearch" Grid.Row="1" Height="35" FontSize="13" 
                 Tag="Поиск по имени или телефону" Margin="0,0,0,10"
                 GotFocus="TxtSearch_GotFocus" LostFocus="TxtSearch_LostFocus"
                 TextChanged="TxtSearch_TextChanged"/>

        <ListBox x:Name="lstClients" Grid.Row="2" Margin="0,10" 
                 DisplayMemberPath="FullName" SelectionChanged="LstClients_SelectionChanged"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button x:Name="btnSelect" Content="ВЫБРАТЬ" Width="100" Height="35"
                    Background="#27AE60" Foreground="White" IsEnabled="False"
                    Click="BtnSelect_Click" Cursor="Hand"/>
            <Button x:Name="btnCancel" Content="ОТМЕНА" Width="100" Height="35" 
                    Margin="15,0,0,0" Background="#95A5A6" Foreground="White"
                    Click="BtnCancel_Click" Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>