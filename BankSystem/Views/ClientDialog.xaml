<Window x:Class="BankSystem.Views.ClientDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Новый клиент" Height="550" Width="500"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ДОБАВЛЕНИЕ КЛИЕНТА" FontSize="16" FontWeight="Bold" 
                   Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="ФИО:*" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox x:Name="txtFullName" Width="330"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Тип клиента:" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbType" Width="330" SelectedIndex="0">
                <ComboBoxItem Content="Физическое лицо" Tag="individual"/>
                <ComboBoxItem Content="Юридическое лицо" Tag="legal"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Паспорт/ИНН:*" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox x:Name="txtPassport" Width="330"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Телефон:*" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox x:Name="txtPhone" Width="330"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Email:" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox x:Name="txtEmail" Width="330"/>
        </StackPanel>

        <StackPanel Grid.Row="6" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Адрес:" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox x:Name="txtAddress" Width="330" Height="60" TextWrapping="Wrap"/>
        </StackPanel>

        <TextBlock x:Name="txtError" Grid.Row="7" Foreground="Red" Margin="100,5,0,0" Visibility="Collapsed"/>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button x:Name="btnSave" Content="СОХРАНИТЬ" Width="100" Height="35"
                    Background="#27AE60" Foreground="White" Click="BtnSave_Click"/>
            <Button x:Name="btnCancel" Content="ОТМЕНА" Width="100" Height="35" 
                    Margin="15,0,0,0" Background="#95A5A6" Foreground="White"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>