<Window x:Class="BankSystem.Views.OpenAccountDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Открытие счета" Height="200" Width="350"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="ОТКРЫТИЕ СЧЕТА" FontSize="16" FontWeight="Bold" 
                   Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Тип счета:" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbAccountType" Width="180" SelectedIndex="0">
                <ComboBoxItem Content="Текущий счет" Tag="current"/>
                <ComboBoxItem Content="Сберегательный счет" Tag="saving"/>
                <ComboBoxItem Content="Карточный счет" Tag="card"/>
            </ComboBox>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button x:Name="btnOk" Content="ОТКРЫТЬ" Width="100" Height="35"
                    Background="#27AE60" Foreground="White" Click="BtnOk_Click"/>
            <Button x:Name="btnCancel" Content="ОТМЕНА" Width="100" Height="35" 
                    Margin="15,0,0,0" Background="#95A5A6" Foreground="White"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>