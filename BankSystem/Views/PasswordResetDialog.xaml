<Window x:Class="BankSystem.Views.PasswordResetDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Сброс пароля" Height="250" Width="400"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="СБРОС ПАРОЛЯ" FontSize="16" FontWeight="Bold" 
                   Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="1" Text="Пользователь:" FontWeight="Bold" Margin="0,5"/>
        <TextBlock x:Name="txtUserLogin" Grid.Row="2" Margin="0,0,0,15" FontSize="14"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Новый пароль:" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <PasswordBox x:Name="txtNewPassword" Width="230"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button x:Name="btnSave" Content="СОХРАНИТЬ" Width="100" Height="35"
                    Background="#27AE60" Foreground="White" Click="BtnSave_Click"/>
            <Button x:Name="btnCancel" Content="ОТМЕНА" Width="100" Height="35" 
                    Margin="15,0,0,0" Background="#95A5A6" Foreground="White"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>