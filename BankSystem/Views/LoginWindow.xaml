<Window x:Class="BankSystem.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в систему" Height="400" Width="500"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        Background="#F0F2F5">

    <Grid Margin="25,25,25,-16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="БАНКОВСКАЯ СИСТЕМА" 
                   FontSize="24" FontWeight="Bold" Foreground="#2C3E50"
                   HorizontalAlignment="Center" Margin="0,0,0,25"/>

        <StackPanel Grid.Row="1" Margin="0,10">
            <TextBlock Text="Логин" FontSize="13" FontWeight="Bold" Margin="5,0"/>
            <TextBox x:Name="txtLogin" Height="38" FontSize="14" Padding="10,5"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,10">
            <TextBlock Text="Пароль" FontSize="13" FontWeight="Bold" Margin="5,0"/>
            <PasswordBox x:Name="txtPassword" Height="38" FontSize="14" Padding="10,5"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,25">
            <Button x:Name="btnLogin" Content="ВОЙТИ" Width="120" Height="40" 
                    Background="#2C3E50" Foreground="White" FontWeight="Bold"
                    Click="BtnLogin_Click" Cursor="Hand"/>
            <Button x:Name="btnCancel" Content="ОТМЕНА" Width="100" Height="40" 
                    Margin="15,0,0,0" Background="#95A5A6" Foreground="White"
                    Click="BtnCancel_Click" Cursor="Hand"/>
        </StackPanel>

        <TextBlock x:Name="txtError" Grid.Row="4" Text="" Foreground="#E74C3C" 
                   HorizontalAlignment="Center" Margin="0,10" Visibility="Collapsed"/>

        <TextBlock Grid.Row="4" Text="Тестовые данные: admin / admin123" 
                   Foreground="Gray" FontSize="11" VerticalAlignment="Top"
                   HorizontalAlignment="Center"/>
    </Grid>
</Window>