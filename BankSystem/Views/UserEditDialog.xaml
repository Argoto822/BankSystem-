<Window x:Class="BankSystem.Views.UserEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Редактирование пользователя" Height="350" Width="450"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="РЕДАКТИРОВАНИЕ ПОЛЬЗОВАТЕЛЯ" FontSize="16" FontWeight="Bold" 
                   Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Логин:" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox x:Name="txtLogin" Width="280" IsReadOnly="True"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Email:" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <TextBox x:Name="txtEmail" Width="280"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Роль:" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <ComboBox x:Name="cmbRole" Width="280" DisplayMemberPath="Name" SelectedValuePath="Id"/>
        </StackPanel>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="0,5">
            <TextBlock Text="Статус:" Width="100" FontWeight="Bold" VerticalAlignment="Center"/>
            <CheckBox x:Name="chkActive" Content="Активен" VerticalAlignment="Center"/>
        </StackPanel>

        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button x:Name="btnSave" Content="СОХРАНИТЬ" Width="100" Height="35"
                    Background="#27AE60" Foreground="White" Click="BtnSave_Click"/>
            <Button x:Name="btnCancel" Content="ОТМЕНА" Width="100" Height="35" 
                    Margin="15,0,0,0" Background="#95A5A6" Foreground="White"
                    Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>